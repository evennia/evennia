# Evennia dependencies, for Windows platform

# windows specific
pypiwin32
# general
django > 1.10, < 2.0
twisted >= 17.0.0
mock >= 1.0.1
pillow == 2.9.0
pytz
future >= 0.15.2
django-sekizai
autobahn >= 17.9.3
