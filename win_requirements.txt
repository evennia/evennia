# Evennia dependencies, for Windows platform

# windows specific
pypiwin32

# general
django > 1.11, < 2.0
twisted >= 18.0.0, < 19.0.0
pillow == 2.9.0
pytz
future >= 0.15.2
django-sekizai
autobahn >= 17.9.3
inflect

# testing
mock >= 1.0.1
anything
